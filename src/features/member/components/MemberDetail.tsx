import { FunctionComponent } from "react";

interface Props {
  className?: string;
}

export const MemberDetail: FunctionComponent<Props> = ({ className }) => {
  return (
    <div>
      <div>MemberDetail</div>
    </div>
  );
};
